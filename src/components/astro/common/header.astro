---
import logo from "@assets/logo.svg";
import OptimizedImage from "@/components/astro/common/OptimizedImage.astro";
import { translations } from "@/lib/i18n/translations";
import type { Locale } from "@/lib/i18n/translations";

interface Props {
    locale: Locale;
}

const { locale } = Astro.props;
const t = translations[locale].nav;
---

<header class="z-50 relative bg-white/80 backdrop-blur-md">
  <nav class="mx-auto flex max-w-7xl items-center justify-between p-4 lg:px-8" aria-label="Global">
    <a href={`/${locale}/`} class="-m-1.5 p-1.5" title="CyberPath">
      <span class="sr-only"> CyberPath </span>
      <OptimizedImage src={logo} alt="CyberPath" class="h-8 w-auto" loading="eager" />
    </a>
    <div class="flex lg:hidden">
      <button
        type="button"
        class="-m-2.5 inline-flex items-center justify-center rounded-md p-2.5 text-gray-700"
        id="mobile-menu-open"
      >
        <span class="sr-only">Open main menu</span>
        <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5"></path>
        </svg>
      </button>
    </div>
    <div class="hidden lg:flex lg:gap-x-10 items-center">
      <a href="#security" class="text-sm font-semibold leading-6 text-gray-900 hover:text-blue-600 transition-colors">{t.security || "Security"}</a>
      <a href="#monitoring" class="text-sm font-semibold leading-6 text-gray-900 hover:text-blue-600 transition-colors">{t.monitoring || "Monitoring"}</a>
      <a href="#pricing" class="text-sm font-semibold leading-6 text-gray-900 hover:text-blue-600 transition-colors">{t.pricing}</a>
      <a href="#reservation" class="text-sm font-semibold leading-6 text-gray-900 hover:text-blue-600 transition-colors">{t.contact || "Contact"}</a>
    </div>
  </nav>
  <div class="hidden lg:hidden" role="dialog" aria-modal="true" id="mobile-menu">
    <div class="fixed inset-0 z-10"></div>
    <div class="fixed inset-y-0 right-0 z-10 w-full overflow-y-auto bg-white px-6 py-6 sm:max-w-sm sm:ring-1 sm:ring-gray-900/10">
      <div class="flex items-center justify-between">
        <a href={`/${locale}/`} class="-m-1.5 p-1.5" title="CyberPath">
          <span class="sr-only"> CyberPath </span>
          <OptimizedImage src={logo} alt="CyberPath" class="h-8 w-auto" loading="eager" />
        </a>
        <button type="button" class="-m-2.5 rounded-md p-2.5 text-gray-700" id="mobile-menu-close">
          <span class="sr-only">Close menu</span>
          <svg class="h-6 w-6 pointer-events-none" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"></path>
          </svg>
        </button>
      </div>
      <div class="mt-6 flow-root">
        <div class="-my-6 divide-y divide-gray-500/10">
          <div class="space-y-2 py-6">
            <a href="#security" class="-mx-3 block rounded-lg px-3 py-2 text-base font-semibold leading-7 text-gray-900 hover:bg-gray-50">{t.security || "Security"}</a>
            <a href="#monitoring" class="-mx-3 block rounded-lg px-3 py-2 text-base font-semibold leading-7 text-gray-900 hover:bg-gray-50">{t.monitoring || "Monitoring"}</a>
            <a href="#pricing" class="-mx-3 block rounded-lg px-3 py-2 text-base font-semibold leading-7 text-gray-900 hover:bg-gray-50">{t.pricing}</a>
            <a href="#reservation" class="-mx-3 block rounded-lg px-3 py-2 text-base font-semibold leading-7 text-gray-900 hover:bg-gray-50">{t.contact || "Contact"}</a>
          </div>
        </div>
      </div>
    </div>
  </div>
</header>
<script is:inline>
  if (!window.mobile) {
    window.mobile = { is_menu_open: false };
  }
  const mobile_menu = document.getElementById("mobile-menu");
  if (!mobile_menu) return;
  const mobile_menu_close = document.getElementById("mobile-menu-close");
  const mobile_menu_open = document.getElementById("mobile-menu-open");
  mobile_menu_close?.addEventListener("click", () => {
    window.mobile.is_menu_open = false;
    mobile_menu.setAttribute("aria-hidden", "true");
    mobile_menu.className = "hidden";
  });
  mobile_menu_open?.addEventListener("click", () => {
    window.mobile.is_menu_open = true;
    mobile_menu.setAttribute("aria-hidden", "false");
    mobile_menu.className = "block";
  });
</script>